{
  "name": "es-mini-agent",
  "version": "0.1.0",
  "private": true,
  "description": "EVRYBDY Studios FLEET — Mini-side HTTP agent. Zero-dependency Node service that the Vercel proxy talks to, with optional OBS Source Record control and demo-mode fallback.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "UNLICENSED"
}
